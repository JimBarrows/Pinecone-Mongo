image: docker:git

stages:
  - deploy

deploy_ui_to_staging:
  stage: deploy
  environment: Staging
  services:
       - docker:dind
  script:
    - docker login registry.gitlab.com
    - docker build -t registry.gitlab.com/reallybigtree/pinecone-mongo .
    - docker push registry.gitlab.com/reallybigtree/pinecone-mongo
  only:
    - develop
